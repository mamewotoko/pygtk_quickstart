dist: xenial
sudo: required
addons:
  apt:
    update: true
    packages:
    - xvfb
    - x11-apps
    - imagemagick
    - ffmpeg
install:
  - sudo bash -x setups.sh
script:
  - bash -x ci/shoot.sh
os:
  - linux
  - osx
